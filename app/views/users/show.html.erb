<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <% if @user.image? %>
          <%= image_tag @user.image.url, class: "user_image" %>
        <% else %>
          <%= image_tag 'default.jpeg', size: '50x50', class: "user_image" %>
        <% end %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>